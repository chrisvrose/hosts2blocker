[package]
name = "hosts2blocker"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.20", features = ["derive"] }
log = "0.4.22"
simple_logger = "5.0.0"

[profile.release]
strip = true